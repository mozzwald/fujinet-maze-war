---
phase: 02-reconciliation-contract
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - clients/atari/maze-war.asm
  - tests/reconciliation_correction_bound_smoke.sh
autonomous: false
requirements:
  - RECN-03
  - RECN-04
gap_closure: true
must_haves:
  truths:
    - "The local Atari wizard no longer leaves stale screen or player/missile fragments behind while moving, replaying, or correcting during normal mixed-session play."
    - "Authoritative correction still settles within about one maze cell in normal play after the visual artifact fix."
    - "Movement and turning still read as original Maze War cadence after the render-state cleanup."
  artifacts:
    - path: "clients/atari/maze-war.asm"
      provides: "Shared authoritative-reposition cleanup for local replay and snap/correction paths"
      contains: "A common helper or equivalent shared label used by NET_LOCAL_REPLAY_PENDING, CKMVAP, and RF_SNAP to erase old actor state before redrawing"
    - path: "tests/reconciliation_correction_bound_smoke.sh"
      provides: "Automated grep/build proof that correction paths share stale-image cleanup and still preserve cadence/replay hooks"
      contains: "grep coverage for NET_LOCAL_REPLAY_PENDING, CKMVAP, RF_SNAP, ERASMAN, NET_GUARD_MASK, NET_ERASE_MASK, INITMOVE_STEP, SETSTIL"
  key_links:
    - from: "clients/atari/maze-war.asm"
      to: "ERASMAN"
      via: "authoritative local/remote reposition helper"
      pattern: "NET_LOCAL_REPLAY_PENDING|CKMVAP|RF_SNAP|ERASMAN"
    - from: "clients/atari/maze-war.asm"
      to: "NET_GUARD_MASK / NET_ERASE_MASK"
      via: "cleanup bookkeeping around correction/redraw"
      pattern: "NET_GUARD_MASK|NET_ERASE_MASK"
    - from: "tests/reconciliation_correction_bound_smoke.sh"
      to: "clients/atari/maze-war.asm"
      via: "grep assertions for cleanup helper usage and preserved replay cadence"
      pattern: "NET_LOCAL_REPLAY_PENDING|CKMVAP|RF_SNAP|INITMOVE_STEP|SETSTIL"
---

<objective>
Close the verified Atari-only blocker in Phase 2 by fixing stale actor cleanup during replay/correction so real movement no longer leaves graphical artifacts on screen, then repeat the blocked Atari/FujiNet approval.

Purpose: The screenshot and debug log show a client-side visual/render-state regression, not a transport failure. Phase 2 only needs the smallest fix that restores clean local movement while preserving bounded correction and original cadence.
Output: Updated Atari correction/render cleanup in `clients/atari/maze-war.asm`, stronger smoke coverage in `tests/reconciliation_correction_bound_smoke.sh`, and a repeated real-Atari checkpoint focused on artifact-free movement.
</objective>

<execution_context>
@/home/mozzwald/.codex/get-shit-done/workflows/execute-plan.md
@/home/mozzwald/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reconciliation-contract/02-VERIFICATION.md
@.planning/phases/02-reconciliation-contract/02-03-PLAN.md
@.planning/phases/02-reconciliation-contract/02-03-SUMMARY.md
@debug/debug.log
@debug/Screenshot from 2026-04-08 17-33-54.png
@clients/atari/maze-war.asm
@tests/reconciliation_correction_bound_smoke.sh
@tests/reconciliation_replay_smoke.sh

<interfaces>
Evidence pointing at a client-side render-state bug, not transport loss:
```text
transport accepted slot=0 format=primary seq=226 joy=0F
transport summary slot=0 ... drop_stale_seq=0 accepted_delta=1250
```

Current local replay rewrites live coordinates and redraw state without an explicit stale-image erase:
```asm
NET_LOCAL_REPLAY_PENDING
	LDX	NET_LOCAL_PID
	...
	LDA	NET_PX_X,X
	STA	LOCX,X
	LDA	NET_PX_Y,X
	STA	LOCY,X
	...
	JSR	NET_CALC_LOC
	LDA	#0
	STA	MOVEST,X
	...
	JSR	SETSTIL
```

Current snap-style correction already erases before teleporting:
```asm
CKMVAP	JSR	ERASMAN
	LDA	NET_PX_X,X
	STA	LOCX,X
	LDA	NET_PX_Y,X
	STA	LOCY,X
	...
	JSR	NET_CALC_LOC
	JSR	SETSTIL
```

Remote snap recovery uses the same erase-then-teleport pattern:
```asm
RF_SNAP
	...
	JSR	ERASMAN
	LDA	NET_PX_X,X
	STA	LOCX,X
	LDA	NET_PX_Y,X
	STA	LOCY,X
	...
	JSR	NET_CALC_LOC
	JSR	SETSTIL
```

`ERASMAN` depends on the old `LOCLO/LOCHI` screen pointer state:
```asm
ERASMAN	LDA	LOCLO,X
	STA	POINTR0
	LDA	LOCHI,X
	STA	POINTR0+1
	...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale actor cleanup in the Atari replay/correction redraw path</name>
  <files>clients/atari/maze-war.asm, tests/reconciliation_correction_bound_smoke.sh</files>
  <read_first>clients/atari/maze-war.asm, .planning/phases/02-reconciliation-contract/02-VERIFICATION.md, .planning/phases/02-reconciliation-contract/02-03-SUMMARY.md, debug/debug.log</read_first>
  <acceptance_criteria>
    - `clients/atari/maze-war.asm` has one shared authoritative-reposition cleanup path, or one clearly equivalent common label, used by `NET_LOCAL_REPLAY_PENDING`, `CKMVAP`, and `RF_SNAP`.
    - The shared cleanup path touches `ERASMAN` before the old actor screen state is invalidated by fresh authoritative `LOCX` or `LOCY` writes.
    - The same cleanup path updates `NET_GUARD_MASK` and any needed `NET_ERASE_MASK` bookkeeping so a replay/correction frame cannot leave stale character or PM fragments behind.
    - `clients/atari/maze-war.asm` still keeps local replay anchored on authoritative `NET_PX_X`/`NET_PX_Y` plus pending-input replay through `INITMOVE_STEP`, `SETSTIL`, and existing movement cadence fields; it does not introduce a new interpolation loop.
    - `tests/reconciliation_correction_bound_smoke.sh` greps for shared cleanup usage from `NET_LOCAL_REPLAY_PENDING`, `CKMVAP`, and `RF_SNAP`, and still rejects local `JMP REMOTE_FOLLOW` or direct replay coordinate-stepping loops.
  </acceptance_criteria>
  <action>Use the screenshot as the defect oracle and `debug/debug.log` as evidence that transport stayed healthy. In `clients/atari/maze-war.asm`, refactor the authoritative reposition code paths that can rewrite actor base position in one VBI (`NET_LOCAL_REPLAY_PENDING`, `CKMVAP`, and `RF_SNAP`) so they share one cleanup sequence that preserves the old draw location long enough to erase it, then recalculates `LOCLO/LOCHI`, marks the slot for any required guard/erase follow-up, and redraws through the existing Maze War movement cadence hooks. The local replay path is the main blocker: it currently rewrites `LOCX/LOCY` and redraws from the new authoritative base without explicitly cleaning the previous screen footprint. Fix that without touching server protocol, combat ordering, pending-ring semantics, or remote smoothing policy beyond the same stale-image cleanup needed for shared reposition logic. Extend `tests/reconciliation_correction_bound_smoke.sh` so it builds the Atari client and greps for the shared cleanup path, the erase/guard bookkeeping symbols it uses, and preserved local replay cadence hooks.</action>
  <verify>
    <automated>bash tests/reconciliation_correction_bound_smoke.sh && bash tests/reconciliation_replay_smoke.sh && make build/maze-war-client</automated>
  </verify>
  <done>All authoritative reposition paths that can visibly move an actor in one VBI now clean the old draw state before redrawing, and automated coverage proves the local replay path still preserves ack-driven replay and original cadence seams.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-run the real Atari/FujiNet movement approval after the cleanup fix</name>
  <files>clients/atari/maze-war.asm, tests/reconciliation_correction_bound_smoke.sh, tests/reconciliation_replay_smoke.sh, tests/reconciliation_snapshot_ack_smoke.sh</files>
  <read_first>.planning/phases/02-reconciliation-contract/02-VERIFICATION.md, .planning/phases/02-reconciliation-contract/02-VALIDATION.md, debug/debug.log, debug/Screenshot from 2026-04-08 17-33-54.png</read_first>
  <acceptance_criteria>
    - The automated commands in `<how-to-verify>` all pass before the mixed-session check begins.
    - The Atari session explicitly exercises steady movement, turn-while-moving, release-to-neutral, wall approach, corner approach, and at least one visible correction case.
    - Approval is only granted if the moving local wizard no longer leaves colored fragments or stale maze-cell ghosts behind during normal movement or correction.
    - Approval is only granted if visible correction still stays within about one maze cell in normal play and movement/turn cadence still feels like original Maze War.
  </acceptance_criteria>
  <action>Repeat the blocked Phase 2 human checkpoint, but focus it on the concrete blocker seen in the screenshot: stale movement artifacts on the Atari display during replay/correction. Do not approve on Linux-only evidence. If the artifact is gone and correction/cadence still satisfy RECN-03 and RECN-04, approve Phase 2. If any movement trails, redraw ghosts, or cadence regressions remain, return a precise symptom report tied to the step where it failed.</action>
  <what-built>Shared Atari cleanup for authoritative local replay and correction redraw paths, plus smoke coverage that guards the fix without changing the reconciliation contract.</what-built>
  <how-to-verify>
    1. Run `bash tests/reconciliation_snapshot_ack_smoke.sh`.
    2. Run `bash tests/reconciliation_replay_smoke.sh`.
    3. Run `bash tests/reconciliation_correction_bound_smoke.sh`.
    4. Run `make all`.
    5. Start the mixed session with 1 Atari client, 1 Linux client, and 2 AI zombies.
    6. On Atari, hold move for several cells, tap turn while moving, release to neutral, then repeat while snapshots continue arriving.
    7. Watch the vacated cells behind the Atari wizard during those moves and turns; there should be no leftover colored sprite fragments or ghost bodies like the saved screenshot.
    8. Drive into a wall and then a corner while prediction is active; confirm correction/replay respects maze boundaries and still leaves no stale draw debris.
    9. Force at least one correction case near a boundary and confirm the wizard settles within about one maze cell and still feels like original Maze War cadence.
  </how-to-verify>
  <verify>
    <automated>bash tests/reconciliation_snapshot_ack_smoke.sh && bash tests/reconciliation_replay_smoke.sh && bash tests/reconciliation_correction_bound_smoke.sh && make all</automated>
  </verify>
  <done>The user either explicitly approves that the Atari movement artifact is gone and RECN-03/RECN-04 now pass, or returns a concrete residual visual/cadence failure report for another targeted gap plan.</done>
  <resume-signal>Type `approved` if the Atari artifact is gone and movement still satisfies RECN-03/RECN-04, or describe the exact remaining visual artifact/cadence failure.</resume-signal>
</task>

</tasks>

<verification>
Use the screenshot and debug log as the baseline defect evidence: transport must stay healthy while the fix removes Atari-local redraw artifacts. Do not treat this as a protocol or server-gap plan unless new code evidence contradicts the current diagnosis.
</verification>

<success_criteria>
Phase 2 can be approved once the real Atari client no longer leaves movement artifacts behind during replay/correction, correction remains bounded in normal play, and original Maze War cadence still holds under client prediction.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reconciliation-contract/02-04-SUMMARY.md`
</output>
