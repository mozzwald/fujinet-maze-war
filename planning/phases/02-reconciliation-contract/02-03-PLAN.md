---
phase: 02-reconciliation-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - clients/atari/maze-war.asm
  - tests/reconciliation_correction_bound_smoke.sh
autonomous: false
requirements:
  - RECN-03
  - RECN-04
must_haves:
  truths:
    - "The local Atari wizard still moves immediately on input and remains close to server truth during normal play."
    - "When correction is needed, the Atari client settles from authoritative state plus replay rather than threshold-only hard snaps."
    - "Local prediction and replay stay aligned with authoritative maze collisions instead of clipping through walls or corners."
    - "Original Maze War move and turn cadence remains intact because replay uses the existing movement engine."
  artifacts:
    - path: "clients/atari/maze-war.asm"
      provides: "Bounded correction policy that preserves the original move/turn and collision pipeline"
      contains: "NET_RECON_P0 guard path plus INITMOVE/MOVEIM/SETSTIL and CHKXDIR/CHKYDIR reuse"
    - path: "tests/reconciliation_correction_bound_smoke.sh"
      provides: "Automated grep/build verification for bounded-correction, collision-preserving, and cadence-preserving hooks"
      contains: "NET_RECON_P0, INITMOVE, MOVEIM, SETSTIL, CHKXDIR, CHKYDIR"
  key_links:
    - from: "clients/atari/maze-war.asm"
      to: "INITMOVE/MOVEIM/SETSTIL"
      via: "replay and correction path"
      pattern: "INITMOVE|MOVEIM|SETSTIL"
    - from: "clients/atari/maze-war.asm"
      to: "CHKXDIR/CHKYDIR"
      via: "movement and collision checks reused during local reconciliation"
      pattern: "CHKXDIR|CHKYDIR|GETAHEDM"
    - from: "tests/reconciliation_correction_bound_smoke.sh"
      to: "clients/atari/maze-war.asm"
      via: "grep for no direct LOCX/LOCY replay writes plus preserved collision/cadence hooks"
      pattern: "NET_RECON_P0|NET_LOCAL_REPLAY_PENDING|MOVEIM|CHKXDIR|CHKYDIR"
---

<objective>
Finish Phase 2 by replacing threshold-only local correction with bounded ack-driven reconciliation that still runs through the original Atari movement cadence, then pause for human verification on real Atari/FujiNet behavior.

Purpose: RECN-03 and RECN-04 are user-visible. This plan makes the reconciliation policy bounded and cadence-preserving, then asks for the only verification automation cannot provide: real visual judgment.
Output: Updated Atari correction policy, `tests/reconciliation_correction_bound_smoke.sh`, and a blocking Atari verification checkpoint.
</objective>

<execution_context>
@/home/mozzwald/.codex/get-shit-done/workflows/execute-plan.md
@/home/mozzwald/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reconciliation-contract/02-RESEARCH.md
@.planning/phases/02-reconciliation-contract/02-VALIDATION.md
@.planning/phases/02-reconciliation-contract/02-02-PLAN.md
@clients/atari/maze-war.asm

<interfaces>
Current local correction seam:
```asm
CKMV_LOC
    LDA HOLDIT
    CMP #NET_RECON_P0
    BCC CKMVCK
    LDA MOVEST,X
    BNE CKMVCK
    JMP REMOTE_FOLLOW
```

Movement cadence primitives to preserve:
```asm
CHKTIME  DEC MOVCLOK,X
SETIME   LDA MOVRATE,X
         STA MOVCLOK,X
         LDA MOVEST,X
         BEQ STRTMOV
         JSR MOVEIM
```

```asm
INITMOVE JSR INITMVE
         ...
         JSR MOVEIM
         JMP CHKSHOT
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make local correction ack-driven, bounded, and cadence-preserving</name>
  <files>clients/atari/maze-war.asm, tests/reconciliation_correction_bound_smoke.sh</files>
  <read_first>clients/atari/maze-war.asm, .planning/phases/02-reconciliation-contract/02-RESEARCH.md, .planning/phases/02-reconciliation-contract/02-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `clients/atari/maze-war.asm` no longer uses `JMP REMOTE_FOLLOW` from the local `CKMV_LOC` branch.
    - `clients/atari/maze-war.asm` keeps `NET_RECON_P0` as a guard threshold, not the primary replay mechanism.
    - `clients/atari/maze-war.asm` preserves calls to `INITMOVE`, `MOVEIM`, and `SETSTIL` in the local replay/correction path.
    - `clients/atari/maze-war.asm` keeps maze-collision checks in the reconciliation path by reusing existing movement/collision seams such as `CHKXDIR`, `CHKYDIR`, or `GETAHEDM` instead of bypassing them.
    - `clients/atari/maze-war.asm` does not contain a replay loop that repeatedly writes `STA LOCX,X` or `STA LOCY,X`.
    - `tests/reconciliation_correction_bound_smoke.sh` exists and greps for the preserved cadence hooks, preserved collision hooks, and the absence of local `JMP REMOTE_FOLLOW`.
  </acceptance_criteria>
  <action>Refactor the local-only correction path in `clients/atari/maze-war.asm` so `CKMV_LOC` stops delegating to `REMOTE_FOLLOW`. Keep `NET_RECON_P0` as the minimum-drift guard for when reconciliation work is necessary, but once a valid ack-based correction is triggered, rebuild the local wizard by reusing the existing cadence engine: `SETSTIL` for facing updates, `INITMOVE` to begin a movement cycle, and `MOVEIM` for the first movement phase. Preserve the existing maze-collision behavior by routing replay/correction through the same movement/collision seams already used for normal motion (`CHKXDIR`, `CHKYDIR`, `GETAHEDM`, or the exact equivalent already on the path); do not invent a bypass that can walk through walls or corners during replay. Do not create a new interpolation or smoothing path, and do not start touching combat, `INITSHOT`, `CHKSHOT`, shot queues, or remote slots. Create `tests/reconciliation_correction_bound_smoke.sh` to run `make build/maze-war-client` and grep for preserved cadence symbols (`INITMOVE`, `MOVEIM`, `SETSTIL`, `MOVRATE`, `MOVCLOK`) plus collision symbols (`CHKXDIR`, `CHKYDIR`, `GETAHEDM`) while also failing if local correction still jumps to `REMOTE_FOLLOW`.</action>
  <verify>
    <automated>bash tests/reconciliation_correction_bound_smoke.sh && make build/maze-war-client</automated>
  </verify>
  <done>The Atari local slot now corrects through authoritative reset plus replay using the original movement and collision engine, with threshold values acting only as bounded guard rails.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify bounded correction and original cadence on real Atari/FujiNet play</name>
  <files>clients/atari/maze-war.asm, tests/reconciliation_snapshot_ack_smoke.sh, tests/reconciliation_replay_smoke.sh, tests/reconciliation_correction_bound_smoke.sh</files>
  <read_first>.planning/phases/02-reconciliation-contract/02-VALIDATION.md, doc/protocol.md, clients/atari/maze-war.asm</read_first>
  <acceptance_criteria>
    - The user runs the three automated commands listed in `<how-to-verify>` before the mixed-session check.
    - The mixed session specifically exercises repeated move, turn, stop, wall-approach, corner-approach, and forced-correction cases for the local Atari slot.
    - Approval is only granted if visible correction stays within about one maze cell during normal play, movement/turn cadence still feels like original Maze War, and replay/correction does not let the local wizard clip through authoritative maze collisions.
  </acceptance_criteria>
  <action>Run the three automated reconciliation smoke scripts first so the checkpoint is only judging the visual Atari behavior that automation cannot cover. Then perform the mixed-session Atari/FujiNet verification steps exactly as written below and block the phase unless the observed correction size and cadence match RECN-03 and RECN-04.</action>
  <what-built>Recipient-specific snapshot ack, Atari pending-input replay, and bounded local correction that reuses the original move/turn engine.</what-built>
  <how-to-verify>
    1. Run `bash tests/reconciliation_snapshot_ack_smoke.sh`.
    2. Run `bash tests/reconciliation_replay_smoke.sh`.
    3. Run `bash tests/reconciliation_correction_bound_smoke.sh`.
    4. Build the repo with `make all`.
    5. Start a mixed session with 1 Atari client, 1 Linux client, and 2 AI zombies.
    6. On the Atari client, hold move, tap turn, release to neutral, then repeat while the Linux client is also moving so authoritative updates keep arriving.
    7. While prediction is active, drive directly into a wall and then into a corner on the Atari client; confirm replay/correction stops on the same maze boundaries the authoritative game uses and never clips through the wall/corner.
    8. Force at least one correction case by briefly causing stale local movement near a maze boundary, then confirm the local wizard settles without a large teleport and the visible correction stays within about one maze cell in normal play.
    9. Confirm movement and turn cadence still matches the original Atari feel rather than a new interpolation loop.
  </how-to-verify>
  <verify>
    <automated>bash tests/reconciliation_snapshot_ack_smoke.sh && bash tests/reconciliation_replay_smoke.sh && bash tests/reconciliation_correction_bound_smoke.sh && make all</automated>
  </verify>
  <done>The user has either approved that real Atari/FujiNet behavior satisfies RECN-03 and RECN-04, including maze-collision fidelity during replay, or returned a concrete visual failure report for follow-up planning.</done>
  <resume-signal>Type `approved` if the Atari behavior matches RECN-03 and RECN-04, including wall/corner collision fidelity during replay, or describe the visible correction/cadence/collision issue that failed.</resume-signal>
</task>

</tasks>

<verification>
Run all three reconciliation smoke scripts plus `make all` before asking for human sign-off. Do not accept the checkpoint on Linux-only evidence; RECN-03 and RECN-04 require real Atari visual confirmation, including wall and corner collision behavior during replay/correction.
</verification>

<success_criteria>
Phase 2 is complete when local Atari movement stays responsive, corrections are bounded and replay-driven, authoritative maze collisions still hold during prediction/replay, and the user confirms the original Maze War movement and turning cadence still holds in a mixed FujiNet session.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reconciliation-contract/02-03-SUMMARY.md`
</output>
