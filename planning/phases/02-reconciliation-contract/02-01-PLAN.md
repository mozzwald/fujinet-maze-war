---
phase: 02-reconciliation-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/main.c
  - clients/linux/main.c
  - clients/linux/sdl_main.c
  - doc/protocol.md
  - tests/reconciliation_snapshot_ack_smoke.sh
autonomous: true
requirements:
  - RECN-01
must_haves:
  truths:
    - "Each SNAPSHOT tells the receiving client which local DELTA sequence the server has already applied for that recipient."
    - "The wire contract exposes ack state without changing combat ordering or adding a second acknowledgement packet."
    - "A real-server smoke check proves slot 0 and slot 1 can receive different ack_seq values in the same test run."
  artifacts:
    - path: "server/main.c"
      provides: "Recipient-specific snapshot ack byte built from authoritative server state"
      contains: "build_snapshot, applied_input_seq, ack_valid"
    - path: "doc/protocol.md"
      provides: "Frozen 20-byte SNAPSHOT contract with ack semantics"
      contains: "bit7 ack_valid and byte 19 ack_seq"
    - path: "tests/reconciliation_snapshot_ack_smoke.sh"
      provides: "Executable snapshot-ack contract check against the real debug server"
      contains: "slot0_ack and slot1_ack assertions"
  key_links:
    - from: "server/main.c"
      to: "doc/protocol.md"
      via: "20-byte PKT_SNAPSHOT layout"
      pattern: "out\\[19\\]|ack_seq|bit7"
    - from: "clients/linux/main.c"
      to: "server/main.c"
      via: "snapshot ack decode/logging"
      pattern: "ack_valid|ack_seq"
    - from: "tests/reconciliation_snapshot_ack_smoke.sh"
      to: "build/maze-war-server"
      via: "debug server launch and UDP packet injection"
      pattern: "maze-war-server --port .* --zombies 0 --debug"
---

<objective>
Freeze the Phase 2 wire contract first: extend SNAPSHOT with a recipient-specific ack byte, make the server publish authoritative applied-input progress, and add a real-server smoke harness that proves the ack is per recipient.

Purpose: RECN-01 is the contract every later replay step depends on. Until the ack byte is explicit and verifiable, Atari reconciliation remains guesswork.
Output: 20-byte SNAPSHOT documentation, server-side applied-input tracking, Linux debug decoding, and `tests/reconciliation_snapshot_ack_smoke.sh`.
</objective>

<execution_context>
@/home/mozzwald/.codex/get-shit-done/workflows/execute-plan.md
@/home/mozzwald/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reconciliation-contract/02-RESEARCH.md
@.planning/phases/02-reconciliation-contract/02-VALIDATION.md
@.planning/phases/01-transport-normalization-and-observability/01-01-SUMMARY.md
@.planning/phases/01-transport-normalization-and-observability/01-02-SUMMARY.md
@server/main.c
@clients/linux/main.c
@clients/linux/sdl_main.c
@doc/protocol.md

<interfaces>
From `server/main.c`:
```c
static void build_snapshot(uint8_t seq, const struct player_state *players,
                           uint8_t *out, size_t out_len) {
  if (out_len < 19) {
    return;
  }
  out[0] = PKT_SNAPSHOT;
  out[1] = seq;
  out[2] = 0x01;
  ...
  out[18] = players[3].score;
}
```

From `clients/linux/main.c`:
```c
} else if (n >= 19 && buf[0] == PKT_SNAPSHOT) {
  int snap_pid = (int)((buf[2] >> 1) & 0x03);
  ...
  players[3].score = buf[18];
}
```

From `doc/protocol.md`:
```text
### 0x40 SNAPSHOT (19 bytes, S->C)
[2] flags
flags bit0: valid
bits1..2: recipient pid
bits3..6: zombie slot bitmask
bit7: reserved
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SNAPSHOT to a 20-byte ack-aware contract</name>
  <files>server/main.c, clients/linux/main.c, clients/linux/sdl_main.c, doc/protocol.md</files>
  <read_first>server/main.c, clients/linux/main.c, clients/linux/sdl_main.c, doc/protocol.md, .planning/phases/02-reconciliation-contract/02-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `doc/protocol.md` says `### 0x40 SNAPSHOT (20 bytes, S->C)`.
    - `doc/protocol.md` defines `flags` bit7 as `ack_valid` and byte `[19]` as `ack_seq`.
    - `server/main.c` contains `have_applied_input_seq` and `applied_input_seq` in `struct client_slot`.
    - `server/main.c` allocates `uint8_t pkt[20];` in the tick loop and writes `out[19] = ack_seq` in `build_snapshot`.
    - `server/main.c` sets `pkt[2]` with bit7 when the recipient has an applied ack and leaves combat, shots, and respawn packet ids unchanged.
    - `clients/linux/main.c` and `clients/linux/sdl_main.c` both parse `ack_valid` from `buf[2] & 0x80` and `ack_seq` from `buf[19]` only when `n >= 20`.
  </acceptance_criteria>
  <action>In `server/main.c`, add two authoritative-truth fields to `struct client_slot`: `uint8_t have_applied_input_seq;` and `uint8_t applied_input_seq;`. Do not reuse `last_delta_seq` as the published ack. After `step_players(...)` and before `build_snapshot(...)`, iterate slots `0..3`; when `clients[i].have_delta_seq` is set, copy `clients[i].last_delta_seq` into `clients[i].applied_input_seq` and set `clients[i].have_applied_input_seq = 1`. Change `build_snapshot()` to require `out_len >= 20`, accept recipient-specific ack inputs, and write byte `19` as `ack_seq`. In the send loop, keep bits0..6 semantics intact and use `pkt[2] |= 0x80` only when the recipient has a valid applied ack. Update both Linux clients to parse the 20-byte snapshot safely, store `ack_valid`/`ack_seq` in local variables, and emit a debug line with the exact prefix `snapshot ack pid=` when `--debug` is enabled. Update `doc/protocol.md` to freeze the 20-byte layout and to state clearly that `ack_seq` is the last local DELTA sequence authoritatively applied for that recipient, not merely the latest received byte-stream sequence.</action>
  <verify>
    <automated>make build/maze-war-server build/maze-war-client build/maze-war-client-sdl</automated>
  </verify>
  <done>The repo has a concrete 20-byte SNAPSHOT contract with a recipient-specific ack byte, and the Linux debug clients understand that contract without introducing any new packet type.</done>
</task>

<task type="auto">
  <name>Task 2: Add a real-server smoke test for recipient-specific ack_seq</name>
  <files>tests/reconciliation_snapshot_ack_smoke.sh</files>
  <read_first>tests/transport_normalize_smoke.sh, server/main.c, doc/protocol.md, .planning/phases/02-reconciliation-contract/02-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `tests/reconciliation_snapshot_ack_smoke.sh` exists and contains `PORT=9103`.
    - The script launches `build/maze-war-server --port "$PORT" --zombies 0 --debug`.
    - The script binds two client sockets and sends canonical DELTAs with exact sequence bytes `7` for slot `0` and `33` for slot `1`.
    - The script fails unless slot `0` receives `ack_valid=1 ack_seq=7` and slot `1` receives `ack_valid=1 ack_seq=33`.
    - The script greps the server log for `TX snapshot -> slot 0` and `TX snapshot -> slot 1` before passing.
  </acceptance_criteria>
  <action>Create `tests/reconciliation_snapshot_ack_smoke.sh` as a POSIX shell script that builds `build/maze-war-server`, starts it on UDP port `9103` with `--zombies 0 --debug`, and runs an inline `python3` UDP harness that creates two client sockets so the server assigns slot `0` and slot `1` independently. Have the harness send a valid `DELTA` packet `[0x41, 7, 0, 0x07]` from socket A and `[0x41, 33, 1, 0x0E]` from socket B`, receive snapshots from each socket, decode `ack_valid = 1 if (flags & 0x80) else 0`, and assert `ack_seq == 7` for the slot-0 recipient and `ack_seq == 33` for the slot-1 recipient. Capture server stdout to a temp log and grep it for `TX snapshot -> slot 0` and `TX snapshot -> slot 1` so the test proves the real server emitted both per-recipient snapshots.</action>
  <verify>
    <automated>bash tests/reconciliation_snapshot_ack_smoke.sh</automated>
  </verify>
  <done>RECN-01 has executable proof: the server publishes per-recipient ack state and the smoke script shows different recipients can receive different `ack_seq` values in the same run.</done>
</task>

</tasks>

<verification>
Keep the quick build green after Task 1. Before closing the plan, run `bash tests/reconciliation_snapshot_ack_smoke.sh` and confirm the smoke output proves the ack is recipient-specific rather than one global sequence byte.
</verification>

<success_criteria>
Phase 2 has a frozen ack contract when the server publishes `ack_seq` inside SNAPSHOT byte `19`, Linux clients decode it safely, and the smoke harness proves slot-specific acknowledgement values against the real debug server.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reconciliation-contract/02-01-SUMMARY.md`
</output>
