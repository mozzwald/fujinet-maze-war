---
phase: 02-reconciliation-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - clients/atari/maze-war.asm
  - tests/reconciliation_replay_smoke.sh
autonomous: true
requirements:
  - RECN-02
must_haves:
  truths:
    - "The Atari client stores local DELTA history instead of forgetting each input after transmit."
    - "When a newer authoritative snapshot arrives, the Atari client discards only inputs with sequence <= ack_seq."
    - "Replay is driven from the authoritative stage-commit seam instead of a parallel simulation path."
  artifacts:
    - path: "clients/atari/maze-war.asm"
      provides: "Pending-input ring, ack staging, discard helper, and replay helper"
      contains: "NET_LOCAL_INPUT_PUSH, NET_LOCAL_ACK_DISCARD, NET_LOCAL_REPLAY_PENDING"
    - path: "tests/reconciliation_replay_smoke.sh"
      provides: "Automated grep/build verification for replay plumbing"
      contains: "NET_PEND_SEQ, NET_PEND_JOY, JSR NET_LOCAL_REPLAY_PENDING"
  key_links:
    - from: "clients/atari/maze-war.asm"
      to: "NET_STAGE_COMMIT"
      via: "authoritative commit then discard/replay"
      pattern: "JSR[[:space:]]+NET_LOCAL_ACK_DISCARD|JSR[[:space:]]+NET_LOCAL_REPLAY_PENDING"
    - from: "clients/atari/maze-war.asm"
      to: "NET_TX_BUILD_DELTA"
      via: "pending ring capture at DELTA construction"
      pattern: "NET_LOCAL_INPUT_PUSH"
---

<objective>
Add the Atari-side replay plumbing that RECN-02 requires: store each local DELTA in a compact pending ring, carry ack bytes through snapshot staging, and rebuild predicted-local state from authoritative truth plus only the still-unacknowledged inputs.

Purpose: Phase 2 fails without local input history. This plan adds that history at the real transmission and stage-commit seams already present in the Atari client.
Output: `clients/atari/maze-war.asm` with pending-input ring and replay helpers, plus `tests/reconciliation_replay_smoke.sh`.
</objective>

<execution_context>
@/home/mozzwald/.codex/get-shit-done/workflows/execute-plan.md
@/home/mozzwald/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reconciliation-contract/02-RESEARCH.md
@.planning/phases/02-reconciliation-contract/02-VALIDATION.md
@.planning/phases/02-reconciliation-contract/02-01-PLAN.md
@clients/atari/maze-war.asm

<interfaces>
Existing Atari seams:
```asm
NET_TX_BUILD_DELTA
    LDA NET_RX_STICK
    ...
    LDA NET_SEQ
    STA NET_TX_BUF+1
    INC NET_SEQ
    LDY NET_LOCAL_PID
    STY NET_TX_BUF+2
```

```asm
NET_SNAP_APPLY
    ...
    LDA NET_SNAP_BUF+2
    LSR
    AND #$03
    STA NET_STAGE_LOCAL_PID
```

```asm
NET_STAGE_COMMIT
    LDA NET_STAGE_SEQ
    ...
    STA NET_STAGE_APPLYSEQ
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ack staging and a compact pending-input ring to the Atari client</name>
  <files>clients/atari/maze-war.asm</files>
  <read_first>clients/atari/maze-war.asm, .planning/phases/02-reconciliation-contract/02-RESEARCH.md, .planning/phases/02-reconciliation-contract/02-01-PLAN.md</read_first>
  <acceptance_criteria>
    - `clients/atari/maze-war.asm` changes `NET_SNAP_BUF` from `.DS 19` to `.DS 20`.
    - `NET_RX_COL40` uses `CPY #20` before calling `NET_SNAP_APPLY`.
    - The file defines `NET_STAGE_ACK_VALID`, `NET_STAGE_ACK_SEQ`, `NET_ACK_VALID`, and `NET_ACK_SEQ`.
    - The file defines `NET_PEND_HEAD`, `NET_PEND_COUNT`, `NET_PEND_SEQ`, and `NET_PEND_JOY`.
    - The file defines a new label `NET_LOCAL_INPUT_PUSH`.
    - `NET_TX_BUILD_DELTA` calls `NET_LOCAL_INPUT_PUSH` after storing `NET_TX_BUF+1` and `NET_TX_BUF+3`.
  </acceptance_criteria>
  <action>In `clients/atari/maze-war.asm`, widen snapshot collection from `19` to `20` bytes so byte `19` can carry the new ack. Add staged/live ack storage symbols exactly named `NET_STAGE_ACK_VALID`, `NET_STAGE_ACK_SEQ`, `NET_ACK_VALID`, and `NET_ACK_SEQ`. Add a fixed-size local pending-input ring with exact symbols `NET_PEND_HEAD`, `NET_PEND_COUNT`, `NET_PEND_SEQ`, and `NET_PEND_JOY`; use eight entries (`.DS 8`) so replay remains compact. In `NET_SNAP_APPLY`, copy `flags bit7` into `NET_STAGE_ACK_VALID` and byte `19` into `NET_STAGE_ACK_SEQ` only when `bit7` is set. Add `NET_LOCAL_INPUT_PUSH` to write the just-built `seq` from `NET_TX_BUF+1` and joy byte from `NET_TX_BUF+3` into the ring in transmit order, advancing `NET_PEND_HEAD` modulo `8` and clamping `NET_PEND_COUNT` at `8` by overwriting the oldest entry when full. Call that helper from `NET_TX_BUILD_DELTA` after the sequence and joy fields are finalized so the stored ring exactly matches what was transmitted.</action>
  <verify>
    <automated>make build/maze-war-client</automated>
  </verify>
  <done>The Atari client can now hold a bounded history of transmitted local inputs and carry snapshot ack metadata through the existing staging pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Replay only unacknowledged inputs at the stage-commit boundary</name>
  <files>clients/atari/maze-war.asm, tests/reconciliation_replay_smoke.sh</files>
  <read_first>clients/atari/maze-war.asm, .planning/phases/02-reconciliation-contract/02-VALIDATION.md, .planning/phases/02-reconciliation-contract/02-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `clients/atari/maze-war.asm` defines labels `NET_LOCAL_ACK_DISCARD` and `NET_LOCAL_REPLAY_PENDING`.
    - `NET_STAGE_COMMIT` copies staged ack into live ack storage and then calls `JSR NET_LOCAL_ACK_DISCARD` followed by `JSR NET_LOCAL_REPLAY_PENDING`.
    - `NET_LOCAL_ACK_DISCARD` compares pending entry sequence bytes against `NET_ACK_SEQ` and drops entries with `seq <= ack_seq` using modulo-256 forward comparison.
    - `NET_LOCAL_REPLAY_PENDING` replays only `NET_PEND_COUNT` entries and does not write directly to `LOCX` or `LOCY` inside the replay loop.
    - `tests/reconciliation_replay_smoke.sh` exists and greps for `NET_LOCAL_INPUT_PUSH`, `NET_LOCAL_ACK_DISCARD`, `NET_LOCAL_REPLAY_PENDING`, `NET_PEND_SEQ`, `NET_PEND_JOY`, and the two `JSR` calls from `NET_STAGE_COMMIT`.
  </acceptance_criteria>
  <action>Add `NET_LOCAL_ACK_DISCARD` and `NET_LOCAL_REPLAY_PENDING` to `clients/atari/maze-war.asm`. In `NET_STAGE_COMMIT`, after `NET_STAGE_APPLYSEQ` is updated, copy `NET_STAGE_ACK_VALID/NET_STAGE_ACK_SEQ` into `NET_ACK_VALID/NET_ACK_SEQ`; if `NET_LOCAL_PID` is valid and `NET_ACK_VALID != 0`, call `NET_LOCAL_ACK_DISCARD` then `NET_LOCAL_REPLAY_PENDING`. `NET_LOCAL_ACK_DISCARD` must walk the oldest-to-newest pending entries, removing only those with sequence bytes authoritatively covered by `NET_ACK_SEQ`. `NET_LOCAL_REPLAY_PENDING` must reset the local slot from staged authoritative state first, then iterate the remaining pending joy bytes and feed them back through the existing local move/turn decision path rather than inventing a new direct-position path; keep replay limited to local input reconstruction and do not touch shot ordering or remote playback semantics. Create `tests/reconciliation_replay_smoke.sh` to run `make build/maze-war-client` and grep the assembly source for the exact replay symbols, the eight-entry ring declarations, and the two `JSR` hooks from `NET_STAGE_COMMIT`.</action>
  <verify>
    <automated>bash tests/reconciliation_replay_smoke.sh && make build/maze-war-client</automated>
  </verify>
  <done>The Atari client now discards acknowledged local inputs and rebuilds predicted-local state from authoritative truth plus only the remaining pending DELTAs.</done>
</task>

</tasks>

<verification>
Keep `make build/maze-war-client` green after each task. Before closing the plan, run `bash tests/reconciliation_replay_smoke.sh` and confirm it proves replay is wired at `NET_STAGE_COMMIT` instead of a side path.
</verification>

<success_criteria>
RECN-02 is satisfied when the Atari client stores local DELTAs, removes entries `<= ack_seq`, and replays only the remaining entries after authoritative snapshot commit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reconciliation-contract/02-02-SUMMARY.md`
</output>
