---
phase: 02-reconciliation-contract
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - clients/atari/maze-war.asm
  - tests/reconciliation_correction_bound_smoke.sh
  - tests/reconciliation_fire_input_smoke.sh
autonomous: false
requirements:
  - RECN-03
  - RECN-04
gap_closure: true
must_haves:
  truths:
    - "Local Atari movement still feels like original Maze War cadence while client prediction is active."
    - "Holding fire and steering toward the shot direction does not create a transient one-cell jump followed by a snap-back."
    - "Replay only rebuilds unacknowledged local movement intent and does not reinterpret fire-only inputs as extra movement steps."
  artifacts:
    - path: "clients/atari/maze-war.asm"
      provides: "Atari-local replay and input interpretation that preserves cadence and separates fire intent from movement replay"
      contains: "NET_TX_BUILD_DELTA, NET_LOCAL_REPLAY_PENDING, NET_LOCAL_REPLAY_STEP, CHKTRIG"
    - path: "tests/reconciliation_fire_input_smoke.sh"
      provides: "Automated grep/build coverage for trigger-bearing input storage and replay gating"
      contains: "NET_TX_BUILD_DELTA, NET_LOCAL_INPUT_PUSH, NET_LOCAL_REPLAY_STEP, INITSHOT, INITMOVE_STEP"
    - path: "tests/reconciliation_correction_bound_smoke.sh"
      provides: "Regression guard that cadence-preserving replay still avoids direct coordinate stepping and stale REMOTE_FOLLOW jumps"
      contains: "NET_LOCAL_REPLAY_PENDING, NET_LOCAL_REPLAY_STEP, MOVRATE, MOVCLOK, INITMOVE_STEP, SETSTIL"
  key_links:
    - from: "clients/atari/maze-war.asm"
      to: "NET_LOCAL_INPUT_PUSH"
      via: "NET_TX_BUILD_DELTA stores raw local joy/trigger intent for later replay"
      pattern: "NET_TX_BUILD_DELTA|NET_LOCAL_INPUT_PUSH|NET_PEND_JOY"
    - from: "clients/atari/maze-war.asm"
      to: "INITMOVE_STEP / SETSTIL / INITSHOT"
      via: "local replay and local input parse distinguish move cadence from fire-only facing intent"
      pattern: "NET_LOCAL_REPLAY_STEP|INITMOVE_STEP|SETSTIL|INITSHOT"
    - from: "tests/reconciliation_fire_input_smoke.sh"
      to: "clients/atari/maze-war.asm"
      via: "grep assertions over trigger-bearing replay and cadence guards"
      pattern: "NET_TX_BUILD_DELTA|NET_LOCAL_REPLAY_STEP|CHKTRIG|INITMOVE_STEP"
---

<objective>
Close the remaining Phase 2 blockers by tightening Atari-local input interpretation around prediction and replay so cadence feels original again and fire-plus-direction no longer causes a transient jump/snap-back.

Purpose: Real Atari evidence now isolates the failure to local cadence and trigger-bearing replay behavior. This plan fixes only that contract and then repeats the blocked hardware checkpoint for RECN-03 and RECN-04.
Output: Updated Atari replay/input handling in `clients/atari/maze-war.asm`, a new fire-input smoke test, and a focused real-Atari cadence/fire verification checkpoint.
</objective>

<execution_context>
@/home/mozzwald/.codex/get-shit-done/workflows/execute-plan.md
@/home/mozzwald/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reconciliation-contract/02-VERIFICATION.md
@.planning/phases/02-reconciliation-contract/02-04-SUMMARY.md
@.planning/phases/02-reconciliation-contract/02-04-PLAN.md
@.planning/phases/02-reconciliation-contract/02-VALIDATION.md
@clients/atari/maze-war.asm
@tests/reconciliation_correction_bound_smoke.sh
@tests/reconciliation_replay_smoke.sh

<interfaces>
Trigger-bearing DELTA bytes are already stored as raw local pending input:
```asm
NET_TX_BUILD_DELTA
	LDA	NET_RX_STICK
	JSR	NET_SAN_STICKA
	STA	NET_TX_BUF+3
	...
	LDA	NET_TX_BUF+3
	ORA	#$10
	STA	NET_TX_BUF+3
	...
	JSR	NET_LOCAL_INPUT_PUSH
```

Current local prediction treats trigger as "fire instead of move":
```asm
CHKTRIG	LDA	NET_RX_TRIG
	BNE	CHKTRG_NF
	JMP	INITSHOT
CHKTRG_NF
	JSR	NET_AHEAD_FREE
	BNE	CHKTRG_BLK
	JMP	INITMOVE
```

Current replay ignores trigger state and replays any directional nibble as movement:
```asm
NET_LOCAL_REPLAY_STEP
	JSR	NET_JOYDIRA
	CMP	#$FF
	BEQ	NLRS_X
	STA	DIR,X
	JSR	NET_AHEAD_FREE
	BNE	NLRS_BLK
	JSR	INITMOVE_STEP
```

Replay currently runs immediately after authoritative commit:
```asm
NET_STAGE_COMMIT
	...
	JSR	NET_LOCAL_ACK_DISCARD
	JSR	NET_LOCAL_REPLAY_PENDING
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Separate fire-only replay from movement replay and restore local cadence gating</name>
  <files>clients/atari/maze-war.asm, tests/reconciliation_correction_bound_smoke.sh, tests/reconciliation_fire_input_smoke.sh</files>
  <read_first>clients/atari/maze-war.asm, .planning/phases/02-reconciliation-contract/02-VERIFICATION.md, .planning/phases/02-reconciliation-contract/02-04-SUMMARY.md, .planning/phases/02-reconciliation-contract/02-VALIDATION.md, tests/reconciliation_correction_bound_smoke.sh, tests/reconciliation_replay_smoke.sh</read_first>
  <acceptance_criteria>
    - `clients/atari/maze-war.asm` keeps local replay rooted at `NET_STAGE_COMMIT` and `NET_LOCAL_REPLAY_PENDING`; it does not add a new transport path or change ack discard ownership.
    - `clients/atari/maze-war.asm` has an explicit fire/trigger-aware branch in local replay so a pending joy byte with trigger asserted cannot fall straight into `INITMOVE_STEP`.
    - `clients/atari/maze-war.asm` preserves local facing updates for trigger-bearing inputs via existing direction/render seams such as `SETSTIL`, but only starts replayed movement when the pending input actually represents movable intent under the existing cadence rules.
    - `clients/atari/maze-war.asm` keeps cadence anchored on existing fields and routines (`MOVRATE`, `MOVCLOK`, `MOVEST`, `INITMOVE_STEP`, `MOVEIM`, `SETSTIL`) rather than inventing a new timer or interpolation loop.
    - `tests/reconciliation_fire_input_smoke.sh` exists, builds the Atari client, and fails if trigger-bearing replay still reaches `INITMOVE_STEP` without a guard or if local fire parsing no longer references the same trigger/move seams.
    - `tests/reconciliation_correction_bound_smoke.sh` still rejects direct `LOCX`/`LOCY` replay stepping and still proves the bounded-correction/cadence hooks remain intact after the replay gating change.
  </acceptance_criteria>
  <action>Use the real-Atari failure as the defect oracle: movement feels too fast overall, and holding fire while steering toward the shot direction can create a one-cell jump then snap-back. In `clients/atari/maze-war.asm`, tighten the local input contract around `NET_TX_BUILD_DELTA`, `CHKTRIG`, `NET_LOCAL_REPLAY_PENDING`, and `NET_LOCAL_REPLAY_STEP` so replay does not reinterpret a trigger-bearing directional packet as guaranteed movement. Preserve the existing authoritative reset plus replay structure from `NET_STAGE_COMMIT`, but make replay distinguish between move intent and fire-only or fire-plus-face intent using the already-stored joy byte rather than adding new wire fields. Keep the original movement cadence engine intact: if replay must restore facing, use existing seams like `SETSTIL`; if replay must restore movement, gate it through the existing cadence state (`MOVRATE`, `MOVCLOK`, `MOVEST`, `INITMOVE_STEP`, `MOVEIM`) instead of collapsing multiple pending inputs into extra immediate motion. Do not edit server protocol, ack publication, ghosting cleanup helpers, pending-ring sizing, combat ordering, or remote-slot follow logic except where the local fire-direction blocker directly requires a shared helper. Add `tests/reconciliation_fire_input_smoke.sh` as a focused grep/build guard for trigger-bearing replay and update `tests/reconciliation_correction_bound_smoke.sh` only as needed to keep the cadence/correction invariants verifiable.</action>
  <verify>
    <automated>bash tests/reconciliation_fire_input_smoke.sh && bash tests/reconciliation_correction_bound_smoke.sh && bash tests/reconciliation_replay_smoke.sh && make build/maze-war-client</automated>
  </verify>
  <done>Atari local replay no longer converts fire-direction intent into an extra movement step, and the replay path still preserves bounded correction plus original movement cadence seams.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-run the real Atari cadence and fire-direction checkpoint</name>
  <files>clients/atari/maze-war.asm, tests/reconciliation_fire_input_smoke.sh, tests/reconciliation_correction_bound_smoke.sh, tests/reconciliation_replay_smoke.sh, tests/reconciliation_snapshot_ack_smoke.sh</files>
  <read_first>.planning/phases/02-reconciliation-contract/02-VERIFICATION.md, .planning/phases/02-reconciliation-contract/02-04-SUMMARY.md, .planning/phases/02-reconciliation-contract/02-VALIDATION.md, clients/atari/maze-war.asm</read_first>
  <acceptance_criteria>
    - The four automated commands in `<how-to-verify>` all pass before the mixed-session checkpoint begins.
    - The Atari session explicitly exercises held movement, repeated turn taps while moving, held fire with directional pressure, and release-to-neutral while authoritative snapshots continue arriving.
    - Approval is only granted if movement and turning no longer feel too fast relative to original Maze War cadence during normal play.
    - Approval is only granted if holding fire while steering toward the shot direction never produces the transient one-cell jump and snap-back seen in the failed checkpoint.
    - Approval is only granted if bounded correction and wall/corner respect remain intact after the local input/replay change.
  </acceptance_criteria>
  <action>Repeat the blocked real-Atari/FujiNet checkpoint with the scope narrowed to the two remaining blockers. Use automation first to confirm the transport/replay contract still holds, then judge only the local Atari user-visible result: cadence feel under prediction and the fire-plus-direction interaction during replay/correction. If either symptom remains, capture the exact input sequence that reproduces it and block the phase again instead of widening the plan into combat or transport work.</action>
  <what-built>Trigger-aware Atari local replay/input interpretation that preserves original movement cadence seams and prevents fire-direction intent from being replayed as a spurious move.</what-built>
  <how-to-verify>
    1. Run `bash tests/reconciliation_snapshot_ack_smoke.sh`.
    2. Run `bash tests/reconciliation_replay_smoke.sh`.
    3. Run `bash tests/reconciliation_correction_bound_smoke.sh`.
    4. Run `bash tests/reconciliation_fire_input_smoke.sh`.
    5. Run `make all`.
    6. Start the mixed session with 1 Atari client, 1 Linux client, and 2 AI zombies.
    7. On Atari, hold movement through multiple cells, release to neutral, and repeat; compare the feel to the original Maze War cadence baseline rather than to transport timing.
    8. While still moving, tap turns and confirm turning cadence stays recognizable and does not feel accelerated.
    9. Hold fire and push a direction toward the intended shot direction multiple times; confirm the wizard does not jump into the next cell and then snap back while firing.
    10. Repeat the same fire-plus-direction input near a wall and a corner; confirm there is still no transient jump and normal bounded correction/wall respect remain intact.
  </how-to-verify>
  <verify>
    <automated>bash tests/reconciliation_snapshot_ack_smoke.sh && bash tests/reconciliation_replay_smoke.sh && bash tests/reconciliation_correction_bound_smoke.sh && bash tests/reconciliation_fire_input_smoke.sh && make all</automated>
  </verify>
  <done>The user either explicitly approves that RECN-03 and RECN-04 now pass on real Atari, or returns a concrete remaining cadence/fire-direction symptom report for a further targeted gap plan.</done>
  <resume-signal>Type `approved` if cadence and fire-direction behavior now satisfy RECN-03/RECN-04 on real Atari, or describe the exact remaining symptom and the input sequence that reproduced it.</resume-signal>
</task>

</tasks>

<verification>
Keep this gap plan local to Atari input interpretation. The automated gate must prove replay, correction, and trigger-bearing input guards remain wired before the real-hardware checkpoint judges cadence and the fire-direction jump regression.
</verification>

<success_criteria>
Phase 2 is ready to close when the Atari local wizard keeps original-feeling move/turn cadence under prediction, holding fire while steering no longer causes a transient move-and-snap-back, and bounded correction still stays within the accepted wall/corner behavior from the latest checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reconciliation-contract/02-05-SUMMARY.md`
</output>
