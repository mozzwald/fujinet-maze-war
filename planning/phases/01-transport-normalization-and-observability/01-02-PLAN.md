---
phase: 01-transport-normalization-and-observability
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - server/main.c
  - server/transport_stats.h
  - server/transport_stats.c
  - tests/transport_counters_smoke.sh
  - tests/README-transport-validation.md
  - doc/protocol.md
  - README.md
autonomous: true
requirements:
  - TRAN-02
must_haves:
  truths:
    - "A tester can separate transport normalization problems from later gameplay or reconciliation problems by reading counters and summary lines."
    - "Per-slot and global transport counters persist long enough to show accepted DELTAs, repaired formats, resyncs, and drop reasons."
    - "The transport contract and debug workflow are documented in the repo so later phases do not reintroduce implicit framing assumptions."
  artifacts:
    - path: "server/transport_stats.h"
      provides: "Transport counter structures and summary logging API"
      exports: ["transport_stats_note_raw_bytes", "transport_stats_log_summary"]
    - path: "tests/transport_counters_smoke.sh"
      provides: "Automated counter/log verification"
      contains: "accepted_delta, delta_swapped, delta_extra_41, delta_resync, drop_bad_joy, drop_stale_seq"
    - path: "doc/protocol.md"
      provides: "Documented canonical ingress contract and debug counters"
      contains: "canonical DELTA form and transport summary fields"
  key_links:
    - from: "server/main.c"
      to: "server/transport_stats.c"
      via: "counter increments and periodic summary logging"
      pattern: "transport_stats_(note|log)_"
    - from: "tests/transport_counters_smoke.sh"
      to: "server debug output"
      via: "grep for counter names and values"
      pattern: "transport summary slot=0"
---

<objective>
Add the observability layer on top of the canonical ingress path: durable counters, debug summaries, and transport contract documentation that make mixed-session failures diagnosable before reconciliation work begins.

Purpose: Satisfy the phase debugging goal so testers can tell whether a failure came from raw transport normalization or later gameplay logic.
Output: Transport stats module, counter verification smoke script, and updated protocol/debug docs.
</objective>

<execution_context>
@/home/mozzwald/.codex/get-shit-done/workflows/execute-plan.md
@/home/mozzwald/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-transport-normalization-and-observability/01-RESEARCH.md
@.planning/phases/01-transport-normalization-and-observability/01-VALIDATION.md
@.planning/phases/01-transport-normalization-and-observability/01-01-SUMMARY.md
@server/main.c
@server/transport_normalize.h
@doc/protocol.md
@README.md

<interfaces>
Expected imports from Plan 01:
```c
enum transport_delta_format { TRANSPORT_DELTA_PRIMARY, TRANSPORT_DELTA_SWAPPED, TRANSPORT_DELTA_EXTRA_41 };

struct transport_delta_packet {
  uint8_t seq;
  uint8_t pid;
  uint8_t joy;
  enum transport_delta_format format;
};
```

Required counter set from research:
```text
raw_datagrams raw_bytes delta_primary delta_swapped delta_extra_41
delta_resync drop_bad_joy drop_stale_seq accepted_delta
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-slot transport counters and periodic debug summaries</name>
  <files>server/main.c, server/transport_stats.h, server/transport_stats.c</files>
  <read_first>server/main.c, server/transport_normalize.h, .planning/phases/01-transport-normalization-and-observability/01-RESEARCH.md, .planning/phases/01-transport-normalization-and-observability/01-01-SUMMARY.md</read_first>
  <acceptance_criteria>
    - `server/transport_stats.h` exports `struct transport_counters` plus `transport_stats_note_raw_bytes(`, `transport_stats_note_delta(`, and `transport_stats_log_summary(`.
    - `server/main.c` increments counters named `raw_datagrams`, `raw_bytes`, `delta_primary`, `delta_swapped`, `delta_extra_41`, `delta_resync`, `drop_bad_joy`, `drop_stale_seq`, and `accepted_delta`.
    - `server/main.c` wires debug summary output through `transport_stats_log_summary(` with the exact prefix `transport summary slot=`.
    - `server/main.c` includes a `2000` ms debug-summary cadence plus disconnect/reclaim summary hooks, leaving executable proof of the emitted line content to Task 2 smoke coverage.
    - Transport counters are updated without changing gameplay tick rate, zombie logic, or snapshot packet layout.
  </acceptance_criteria>
  <action>Create `server/transport_stats.h` and `server/transport_stats.c` to own per-slot and global transport counters plus summary formatting. Store one `struct transport_counters` per `client_slot` and one global aggregate in `server/main.c`. Increment `raw_datagrams` and `raw_bytes` before byte ingestion, increment `delta_primary`, `delta_swapped`, or `delta_extra_41` from the canonical DELTA format chosen by Plan 01, increment `delta_resync` when the normalizer shifts the 4-byte window, increment `drop_bad_joy` and `drop_stale_seq` on those exact drop reasons, and increment `accepted_delta` only after the DELTA is accepted for gameplay mutation. Add a `last_transport_summary_ms` timer in the server loop and print `transport summary slot=%d ...` every `2000` ms under `--debug`, plus a final per-slot dump before clearing or reusing a slot.</action>
  <verify>
    <automated>make build/maze-war-server build/maze-war-client build/maze-war-client-sdl</automated>
  </verify>
  <done>The server has a compiled transport-stats layer wired into the canonical ingress path, with executable behavioral proof of exact summary content deferred to Task 2 smoke coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add counter smoke coverage and freeze the transport contract in docs</name>
  <files>tests/transport_counters_smoke.sh, tests/README-transport-validation.md, doc/protocol.md, README.md</files>
  <read_first>tests/transport_normalize_smoke.sh, tests/README-transport-validation.md, doc/protocol.md, README.md, .planning/phases/01-transport-normalization-and-observability/01-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `tests/transport_counters_smoke.sh` exists and contains checks for `transport summary slot=0`.
    - The smoke script greps exact counter/value pairs `delta_swapped=1`, `delta_extra_41=1`, `drop_bad_joy=1`, `drop_stale_seq=1`, and `accepted_delta=2`.
    - `doc/protocol.md` documents the internal canonical DELTA form as `{type=0x41, seq, pid=slot, joy}` and names the supported normalization counters.
    - `README.md` references both `tests/transport_normalize_smoke.sh` and `tests/transport_counters_smoke.sh` in the debugging workflow.
    - `tests/README-transport-validation.md` includes the exact summary prefixes `transport accepted` and `transport summary`.
  </acceptance_criteria>
  <action>Create `tests/transport_counters_smoke.sh` as a shell script that starts the debug server, injects a canonical DELTA, a swapped DELTA, an extra-leading-`0x41` DELTA, one invalid-joy DELTA, and one stale-sequence DELTA for `slot=0`, then fails unless the captured summary log contains `delta_swapped=1`, `delta_extra_41=1`, `drop_bad_joy=1`, `drop_stale_seq=1`, and `accepted_delta=2`. Update `doc/protocol.md` to distinguish wire formats from the internal canonical DELTA representation and to list the transport summary counters by name. Update `README.md` and `tests/README-transport-validation.md` so mixed-session debugging tells users to run both smoke scripts first, then collect server logs with the documented `transport accepted` and `transport summary` prefixes during Atari/FujiNet validation.</action>
  <verify>
    <automated>bash tests/transport_counters_smoke.sh && make all</automated>
  </verify>
  <done>Transport observability is executable and documented: the smoke script checks exact counter values and the repo docs describe how to separate transport failures from later gameplay faults.</done>
</task>

</tasks>

<verification>
After each task, keep the quick build green. Before closing the plan, run `bash tests/transport_counters_smoke.sh && make all` and confirm the debug summary includes the researched counter names rather than one-off event spam.
</verification>

<success_criteria>
`TRAN-02` is satisfied for Phase 1 when mixed-session testers can read stable `transport accepted` and `transport summary` output, tie exact counter changes to transport events, and use documented smoke scripts to reproduce those signals before manual Atari validation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-transport-normalization-and-observability/01-02-SUMMARY.md`
</output>
