---
phase: 01-transport-normalization-and-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/main.c
  - server/transport_normalize.h
  - server/transport_normalize.c
  - tests/transport_normalize_smoke.sh
  - tests/README-transport-validation.md
autonomous: true
requirements:
  - TRAN-01
must_haves:
  truths:
    - "Server routes Linux and Atari-originated DELTA input through one canonical normalization boundary before mutating gameplay input state."
    - "Canonical DELTA, swapped seq/pid DELTA, and extra-leading-0x41 NetStream DELTA all decode into the same internal packet shape."
    - "An automated smoke script exists for the canonical ingress path and fails if normalization stops accepting the supported variants."
  artifacts:
    - path: "server/transport_normalize.h"
      provides: "Transport normalization contracts and canonical DELTA types"
      exports: ["transport_rx_push_byte", "transport_decode_delta_for_slot"]
    - path: "server/transport_normalize.c"
      provides: "Byte-stream framing repair and DELTA normalization logic"
      contains: "extra-leading-0x41, swapped seq/pid, resync handling"
    - path: "tests/transport_normalize_smoke.sh"
      provides: "Automated DELTA normalization smoke coverage"
      contains: "primary, swapped, and extra-0x41 cases"
  key_links:
    - from: "server/main.c"
      to: "server/transport_normalize.c"
      via: "process_client_bytes and DELTA dispatch"
      pattern: "transport_(rx_push_byte|decode_delta_for_slot)"
    - from: "tests/transport_normalize_smoke.sh"
      to: "build/maze-war-server"
      via: "debug server launch plus UDP packet injection"
      pattern: "maze-war-server --port .* --zombies 0 --debug"
---

<objective>
Create the first executable transport contract for Phase 1: a Wave 0 smoke harness plus a dedicated server normalization boundary that canonicalizes DELTA input before gameplay state changes.

Purpose: Freeze the ingress seam now so later reconciliation work is not debugging raw FujiNet framing behavior inside gameplay handlers.
Output: New transport-normalization module, server wiring that uses it, and an automated smoke script that proves the accepted DELTA variants.
</objective>

<execution_context>
@/home/mozzwald/.codex/get-shit-done/workflows/execute-plan.md
@/home/mozzwald/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-transport-normalization-and-observability/01-RESEARCH.md
@.planning/phases/01-transport-normalization-and-observability/01-VALIDATION.md
@server/main.c
@doc/protocol.md
@README.md

<interfaces>
From `server/main.c`:
```c
struct client_slot {
  int in_use;
  struct sockaddr_in addr;
  socklen_t addr_len;
  uint64_t last_seen_ms;
  int sent_bricks;
  uint8_t rx_need;
  uint8_t rx_idx;
  uint8_t rx_buf[8];
  uint8_t have_delta_seq;
  uint8_t last_delta_seq;
};

static int sanitize_client_joy(uint8_t raw, uint8_t *out);
static int delta_seq_is_fresh(struct client_slot *c, uint8_t seq);
static void handle_client_packet(int slot, const uint8_t *pkt, size_t len,
                                 struct player_state *players, uint8_t *brick_bits,
                                 int sock, struct client_slot *clients,
                                 uint8_t *seq, int debug,
                                 uint64_t now, uint64_t *last_input_ms);
static void process_client_bytes(int slot, const uint8_t *buf, size_t n,
                                 struct player_state *players, uint8_t *brick_bits,
                                 int sock, struct client_slot *clients,
                                 uint8_t *seq, int debug,
                                 uint64_t now, uint64_t *last_input_ms);
```

From `doc/protocol.md`:
```text
Primary DELTA: [0x41][seq][pid][joy]
Compatibility DELTA: [0x41][pid][seq][joy]
Byte-stream DELTA: [0x41][0x41][seq][pid][joy]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 transport normalization smoke coverage</name>
  <files>tests/transport_normalize_smoke.sh, tests/README-transport-validation.md</files>
  <read_first>.planning/phases/01-transport-normalization-and-observability/01-VALIDATION.md, .planning/phases/01-transport-normalization-and-observability/01-RESEARCH.md, README.md, doc/protocol.md</read_first>
  <acceptance_criteria>
    - `tests/transport_normalize_smoke.sh` exists and contains `PORT=9101`.
    - The script launches `build/maze-war-server --port "$PORT" --zombies 0 --debug`.
    - The script sends three DELTA cases: `primary`, `swapped`, and `extra-0x41`.
    - The script greps for exact debug markers `transport accepted slot=0 format=primary`, `transport accepted slot=0 format=swapped`, and `transport accepted slot=0 format=extra-41`.
    - `tests/README-transport-validation.md` documents the server log file used by the smoke scripts plus the manual Atari mixed-session capture flow.
  </acceptance_criteria>
  <action>Create `tests/transport_normalize_smoke.sh` as a POSIX shell script that builds `build/maze-war-server`, runs it on UDP port `9101` with `--zombies 0 --debug`, injects three DELTA payloads from localhost via an inline `python3` UDP sender, captures server stdout to a temp log, and exits nonzero unless the log contains one accepted `primary`, one accepted `swapped`, and one accepted `extra-41` normalization line for `slot=0`. Create `tests/README-transport-validation.md` with exact commands for running the smoke script, storing the server log beside the script output, and the manual Atari/FujiNet validation notes that pair the Atari session with the same server log capture.</action>
  <verify>
    <automated>bash tests/transport_normalize_smoke.sh</automated>
  </verify>
  <done>The phase has its first executable transport ingress smoke check and a documented manual log-capture flow for Atari mixed-session validation.</done>
</task>

<task type="auto">
  <name>Task 2: Extract canonical DELTA normalization into a dedicated server module</name>
  <files>server/main.c, server/transport_normalize.h, server/transport_normalize.c</files>
  <read_first>server/main.c, doc/protocol.md, .planning/phases/01-transport-normalization-and-observability/01-RESEARCH.md, tests/transport_normalize_smoke.sh</read_first>
  <acceptance_criteria>
    - `server/transport_normalize.h` exports `enum transport_rx_result`, `enum transport_delta_format`, `struct transport_rx_state`, and `struct transport_delta_packet`.
    - `server/transport_normalize.c` implements `transport_rx_push_byte(` and `transport_decode_delta_for_slot(`.
    - `server/main.c` calls `transport_rx_push_byte(` from `process_client_bytes(` and `transport_decode_delta_for_slot(` before any `players[pid].joy = joy;` mutation.
    - `server/main.c` no longer contains the inline comment `Some Atari netstream paths prepend an extra 0x41 byte before DELTA`.
    - Debug acceptance logs use the exact prefix `transport accepted slot=` with `format=primary|swapped|extra-41`.
  </acceptance_criteria>
  <action>Create `server/transport_normalize.h` and `server/transport_normalize.c` to own byte-stream framing and DELTA compatibility decoding. Define `struct transport_rx_state { uint8_t need; uint8_t idx; uint8_t buf[8]; };`, `enum transport_delta_format { TRANSPORT_DELTA_PRIMARY, TRANSPORT_DELTA_SWAPPED, TRANSPORT_DELTA_EXTRA_41 };`, and `struct transport_delta_packet { uint8_t seq; uint8_t pid; uint8_t joy; enum transport_delta_format format; };`. Move the extra-leading-`0x41` detection, 4-byte resync shift, swapped `seq/pid` decode, and joy-byte validation decision into that module. Update `struct client_slot` in `server/main.c` to store `struct transport_rx_state rx;` instead of raw `rx_need/rx_idx/rx_buf` fields, keep slot identity authoritative, and make `handle_client_packet()` consume canonicalized DELTA packets only. When `--debug` is enabled, log canonical acceptance as `transport accepted slot=%d format=%s seq=%u joy=%02X` immediately before gameplay input state is written.</action>
  <verify>
    <automated>bash tests/transport_normalize_smoke.sh && make build/maze-war-server build/maze-war-client build/maze-war-client-sdl</automated>
  </verify>
  <done>The server has a dedicated normalization boundary, gameplay input mutation no longer contains transport compatibility parsing, and the smoke script passes for all supported DELTA variants.</done>
</task>

</tasks>

<verification>
Run `bash tests/transport_normalize_smoke.sh` after each commit in this plan. At plan completion, confirm `make build/maze-war-server build/maze-war-client build/maze-war-client-sdl` still succeeds so the canonical ingress refactor did not break Linux clients.
</verification>

<success_criteria>
`TRAN-01` is satisfied for the Phase 1 transport boundary when DELTA ingress is canonicalized before gameplay mutation, and the accepted Linux plus Atari-compatible wire forms are proven by the smoke script instead of ad hoc manual observation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-transport-normalization-and-observability/01-01-SUMMARY.md`
</output>
