---
phase: 04-render-state-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - clients/atari/maze-war.asm
  - tests/input_send_cadence_smoke.sh
autonomous: false
requirements:
  - RNDR-01
must_haves:
  truths:
    - "One transmitted DELTA corresponds to exactly one predicted local cell, in every state: moving, blocked, turning and idle."
    - "The movement decision and the transmit slot are driven by the same event, so their phase cannot slip."
    - "Corrections during sustained cornering drop measurably against the current build, measured the same way on the same rig."
  artifacts:
    - path: "clients/atari/maze-war.asm"
      provides: "Movement decision gated on the transmit tick"
      contains: "a permission flag set at the periodic slot and consumed by the local move decision"
    - path: "tests/input_send_cadence_smoke.sh"
      provides: "Guard for the one-delta-per-cell invariant"
      contains: "assertions on the transmit site and the movement gate staying coupled"
  key_links:
    - from: "clients/atari/maze-war.asm"
      to: "build/maze-war.lab"
      via: "diagnostic counters used to compare correction rates"
      pattern: "NET_DIAG_CNT|NET_DIAG_SNAPS"
---

# 04-01 — Lock the movement decision to the transmit tick

## Why

`NET_FRAME_DIV` (6 frames) and the cell period built from `MOVRATE` (also 6
frames) are the same rate but free-running in phase, and the phase
re-randomises on every stop, turn or block, because `PLRMVE` re-runs every
`MOVRATE` frames whenever `MOVEST` is 0. When they slip, one transmit window
contains two move decisions or none, so the client predicts a different number
of cells than the server runs. That is the residual correction source after
`b8d80d2`: roughly 3 corrections per 40s of scripted hard cornering, down from
about 11.

Making one event drive both removes the slip by construction rather than
reducing it.

## Shape

Set a "may start a cell" permission at the periodic transmit slot, and have the
local move decision consume it. The animation keeps running on `MOVRATE` for
smooth rendering; only the decision to begin a new cell is gated.

## Watch out for

- A cell currently takes 6 frames and the window is 6 frames — zero margin. If
  an animation overruns, the next decision arrives late and movement stutters
  instead of desynchronising. Shaving the cell (for example `MOVRATE` 1 for the
  later phases) buys slack but changes how the game feels, so it is the user's
  call, not a silent adjustment.
- Two earlier attempts at this coupling failed and are recorded in STATE.md:
  moving the *send* onto the movement clock starved the server (it transmits
  nothing during the four-phase animation), and publishing at the move
  *decision* fired up to 30 times a second while blocked or idle. This plan
  moves the *decision* onto the transmit clock, which is the remaining
  direction and the one that cannot starve the link.

## Done when

- The correction counters, measured with the existing 40s scripted cornering
  run, are materially below the current build on the same rig.
- Movement still feels right to the user on real hardware. This plan is
  `autonomous: false` for that reason: the metric can improve while the game
  feels worse, and only the user can judge the second half.

## Result

Implemented as a one-byte permission, `NET_MOVE_DUE`. The periodic slot raises
it immediately after `JSR NET_TX_BUILD_DELTA`, so it inherits that call's skip
when the transmitter is busy: only a DELTA that actually left the machine
licenses a cell. `STRTMOV` consumes it before falling through to `PLRMVE`, and
returns to `CHKSHOT` without starting a cell when it is clear. It is a flag,
not a counter, deliberately — two grants arriving during one long animation
collapse to one, so the client can under-move by a cell (which reconciliation
already handles) but can never bank a step and over-move.

Measured with `corner40.py`: 140 scripted direction changes x 17 emulated
frames, six runs each side, same emulator session and the same server
(`--zombies 0`).

| build    | cells | corrections | per 100 cells |
| -------- | ----- | ----------- | ------------- |
| baseline | 1153  | 25          | 2.17          |
| gated    | 1156  | 8           | 0.69          |

Per-run baseline 0.0 / 2.6 / 3.6 / 0.5 / 0.0 / 6.2; gated 0.0 / 1.0 / 0.0 /
0.0 / 0.5 / 2.6. Both spreads are wide, which is why the totals are the
comparison and not any single pair.

Cells moved is the control, and it is the number that would have exposed a
cheat: a gate that simply moved less would have bought the correction rate at
the cost of the game. 1156 against 1153 says the client still starts a cell for
every transmit slot; it has stopped starting one on the wrong side of the slot
that describes it.

Guarded by two new assertions in `tests/input_send_cadence_smoke.sh`, each
checked to fail when its half of the coupling is removed.

Still open: the user's hardware test. The metric cannot tell us whether
movement now stutters, and this plan is `autonomous: false` for that reason.
