---
phase: 04-render-state-separation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - clients/atari/maze-war.asm
  - tests/font_coverage_smoke.sh
autonomous: true
requirements:
  - RNDR-01
must_haves:
  truths:
    - "Every character a player can type into the name field renders as that character on the scoreboard."
    - "Every character used by the status and prompt messages renders correctly in whichever charset is active when it is drawn."
    - "A regression test fails if a glyph slot used by text is overwritten with artwork again."
  artifacts:
    - path: "clients/atari/maze-war.asm"
      provides: "Complete A-Z and 0-9 glyphs in the embedded charset"
      contains: "letter glyphs at the screen codes for F, J, Q, V and X"
    - path: "tests/font_coverage_smoke.sh"
      provides: "Guard that the embedded font covers the printable range text uses"
      contains: "per-glyph check over the screen codes reachable from HOST_SCR"
  key_links:
    - from: "clients/atari/maze-war.asm"
      to: "tests/font_coverage_smoke.sh"
      via: "charset coverage for user-supplied names"
      pattern: "CHRSET|HOST_SCR|NET_NAME_DRAW"
---

# 04-05 — Complete the embedded font for the characters text can use

## Why

Reported from real hardware 2026-09-04: the name `MOZZXL` renders with three
dots in place of the `X`, and the `V` in "CONNECTING TO SERVER" is a corrupt
glyph.

Both are the same cause, confirmed by dumping the embedded charset at `$4000`
against the screen codes `HOST_SCR` produces:

- `X` (screen code `$38`) is three isolated dots down the left edge — exactly
  the reported artefact.
- `V` (screen code `$36`) is a dense checkerboard.
- `F`, `J` and `Q` are likewise artwork rather than letters.
- `S`, `C`, `G`, `K` and the rest of A-Z and 0-9 are proper letters, which is
  why `WIZARD`, `ZOMBIE` and `MOZZ` have always looked right.

The five broken slots are precisely the letters the original game's own text
never used, so their glyph slots were reused for maze and sprite artwork. That
was safe when all text was compile-time constants. It stopped being safe when
player names became arbitrary user input.

## Shape

Draw letterforms for `F`, `J`, `Q`, `V`, `X` in the existing style (the font is
bolder and squarer than the ROM font — match it, do not paste ROM glyphs), and
move any artwork currently occupying those slots to codes outside the range
`HOST_SCR` can produce.

## Watch out for

- Check what actually uses those artwork glyphs before moving them. They are
  reachable from the maze, shot and wizard drawing paths, and a slot that looks
  like noise in a font dump may be a deliberate sprite tile.
- The prompt screen switches `CHBASE` to the ROM charset (`HOST_BOOT` writes
  `#$E0`) while the scoreboard uses the embedded font. Confirm which charset is
  active when each status message is drawn — the reported `V` corruption implies
  at least one message is drawn under the embedded font, so fixing the glyph may
  fix it, but the charset-selection path should be checked rather than assumed.
- The name field should reject or fold characters the font cannot represent
  rather than drawing garbage, as a backstop for anything missed.

## Done when

- A name using all of `F J Q V X` renders correctly on the scoreboard.
- "CONNECTING TO SERVER" renders correctly on hardware.
- `font_coverage_smoke.sh` fails if any screen code reachable from `HOST_SCR`
  maps to a glyph that is not the expected character.

## Result

`6932c06`. The diagnosis held, with one addition: **H is also artwork**, which
the plan had not listed — six broken letters, not five. Verified by dumping the
charset from the assembled binary rather than trusting the plan.

The relocation was harder than "move it somewhere free". The only blank slots
are `$00`, `$08-$0F` and `$5F`, and `$5F` is the only one unreachable from
`HOST_SCR`. Moving the artwork into `$08-$0D` just moved the bug — those codes
are `( ) * + , -` and equally reachable — and the font test caught that
immediately. Final placement is `$08-$0C` and `$0F`, deliberately skipping `$0D`
and `$0E` because those are `-` and `.`, which host names need and which were
blank slots drawing nothing; both got real glyphs.

Five artwork slots stay reachable in principle (`$1E`, `$1F`, `$3B-$3F` = `>?`
and `[\]^_`) and have no letter to be, so `HOST_SCR` ends in a whitelist rather
than a blacklist.

The plan's charset warning was well placed: the prompt screen runs on the ROM
charset (`HOST_BOOT` sets `CHBASE` `$E0`), so a screenshot of `NAME: FJQVXH`
there proves nothing about the embedded font. Verified in-game instead, where
the scoreboard uses the embedded charset and reads `FJQVXH` correctly.

## Note

The name draw path calls `HOST_SCR`, so the fix reaches it. While confirming
that, a separate oddity appeared in scoreboard memory: raw lowercase ASCII
("wizard", `$77 $69 $7A...`) sitting in columns that would render as maze
artwork. It was not chased down and is not part of this plan. Worth a look.
