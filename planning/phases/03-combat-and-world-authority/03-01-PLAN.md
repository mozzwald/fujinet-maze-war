---
phase: 03-combat-and-world-authority
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/main.c
  - doc/protocol.md
  - tests/combat_ordering_smoke.sh
  - tests/combat_world_authority_smoke.sh
autonomous: true
requirements:
  - COMB-01
  - COMB-03
  - WRLD-01
must_haves:
  truths:
    - "The server exposes one explicit same-tick contract for respawn, fire, movement, shot stepping, hits, and brick destruction."
    - "Client fire remains intent-only; projectile origin and immediate combat/world outcomes are always derived from authoritative server state."
    - "Automated smoke coverage proves move-then-fire, turn-then-fire, immediate adjacent hit, and brick-destruction behavior against the real server."
  artifacts:
    - path: "server/main.c"
      provides: "Frozen authoritative combat/world ordering"
      contains: "explicit action order helpers or comments plus authoritative event emission"
    - path: "doc/protocol.md"
      provides: "Documented combat/world contract"
      contains: "same-tick ordering semantics for SHOT, BRICK_DELTA, RESPAWN, and SNAPSHOT"
    - path: "tests/combat_ordering_smoke.sh"
      provides: "Automated server ordering smoke coverage"
      contains: "move-then-fire, turn-then-fire, immediate hit, fire-into-brick"
    - path: "tests/combat_world_authority_smoke.sh"
      provides: "Automated score/respawn/brick authority coverage"
      contains: "score increment, respawn pending/final, brick mutation"
  key_links:
    - from: "server/main.c"
      to: "doc/protocol.md"
      via: "authoritative combat/world contract"
      pattern: "start_shot|step_shots|step_players|RESPAWN|SHOT|BRICK_DELTA"
    - from: "tests/combat_ordering_smoke.sh"
      to: "build/maze-war-server"
      via: "debug server launch and deterministic packet/assert flow"
      pattern: "maze-war-server --port .* --zombies .* --debug"
---

<objective>
Freeze Phase 3's server-side combat/world contract first: make the authoritative same-tick order explicit, document it in `doc/protocol.md`, and create Wave 0 smoke harnesses that prove combat outcomes come from the server rather than client-local heuristics.

Purpose: Later Atari/Linux client work needs one clear contract for bullet origin, brick mutation, score changes, death, and respawn. Phase 3 should not guess at this from scattered handlers.
Output: Updated `server/main.c` and `doc/protocol.md`, plus `tests/combat_ordering_smoke.sh` and `tests/combat_world_authority_smoke.sh`.
</objective>

<execution_context>
@/home/mozzwald/.codex/get-shit-done/workflows/execute-plan.md
@/home/mozzwald/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-combat-and-world-authority/03-RESEARCH.md
@.planning/phases/03-combat-and-world-authority/03-VALIDATION.md
@server/main.c
@doc/protocol.md

<interfaces>
From `server/main.c`:
```c
static void start_shot(int shooter, struct player_state *players,
                       struct shot_state *shots, uint8_t *bricks,
                       int sock, struct client_slot *clients, uint8_t *seq,
                       uint8_t joy, int debug);
static void step_shots(struct player_state *players, struct shot_state *shots,
                       uint8_t *bricks, int sock,
                       struct client_slot *clients, uint8_t *seq,
                       int debug);
static void step_players(struct player_state *players, struct shot_state *shots,
                         uint8_t *bricks, int sock,
                         struct client_slot *clients, uint8_t *seq,
                         int debug, int zombies,
                         const uint64_t *last_input_ms);
```

From `doc/protocol.md`:
```text
0x40 SNAPSHOT => authoritative position/joy/score
0x42 SHOT => authoritative projectile state
0x51 BRICK_DELTA => authoritative brick removal
0x52 RESPAWN => pending/final respawn state
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze and document the authoritative same-tick combat order</name>
  <files>server/main.c, doc/protocol.md, tests/combat_ordering_smoke.sh</files>
  <read_first>server/main.c, doc/protocol.md, .planning/phases/03-combat-and-world-authority/03-RESEARCH.md, .planning/phases/03-combat-and-world-authority/03-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `tests/combat_ordering_smoke.sh` exists and launches `build/maze-war-server` in debug mode.
    - The script covers move-then-fire, turn-then-fire, immediate adjacent hit, and fire-into-brick cases.
    - `doc/protocol.md` explicitly describes the same-tick order for respawn resolution, fire evaluation, movement gating while firing directionally, and shot stepping.
    - `server/main.c` exposes the authoritative order clearly enough that the smoke script can anchor on stable debug markers or helper seams instead of inference.
  </acceptance_criteria>
  <action>Create `tests/combat_ordering_smoke.sh` as a deterministic shell harness that builds and runs `build/maze-war-server` on a fixed UDP port in `--debug` mode, sends canonical DELTA sequences from one or more localhost clients, and asserts the server log proves the intended order for move-then-fire, turn-then-fire, immediate adjacent hit, and fire-into-brick behavior. In `doc/protocol.md`, add a dedicated combat/world semantics subsection that spells out the same-tick contract: respawns finalize first, fire intent uses authoritative current wizard state, directional fire suppresses same-tick movement, and then active shots step. In `server/main.c`, make that order explicit and stable for tests, either by extracting small helper seams or by adding concise contract comments plus debug logging around the existing sequence without changing the gameplay model.</action>
  <verify>
    <automated>bash tests/combat_ordering_smoke.sh && make build/maze-war-server</automated>
  </verify>
  <done>The authoritative same-tick combat contract is no longer implicit, and a focused smoke harness proves the server still executes that order.</done>
</task>

<task type="auto">
  <name>Task 2: Prove authoritative score, respawn, and brick outcomes</name>
  <files>server/main.c, doc/protocol.md, tests/combat_world_authority_smoke.sh</files>
  <read_first>server/main.c, doc/protocol.md, tests/combat_ordering_smoke.sh, .planning/phases/03-combat-and-world-authority/03-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `tests/combat_world_authority_smoke.sh` exists and asserts score increment, pending respawn, final respawn, and brick removal behavior against the real server.
    - `doc/protocol.md` explicitly states that score, death, respawn, and brick mutation are server-authoritative outcomes.
    - `server/main.c` emits stable authoritative behavior for immediate hit, moving-shot hit, and non-border brick destruction without requiring client-local derivation.
  </acceptance_criteria>
  <action>Create `tests/combat_world_authority_smoke.sh` to launch the real server, drive deterministic scenarios that cause an immediate hit, a moving-shot hit, and a shot-vs-brick collision, then fail unless the observed packets or debug log prove the server issued the score change, `RESPAWN` pending/final events, and `BRICK_DELTA` mutation itself. In `doc/protocol.md`, add explicit notes that `SNAPSHOT` score bytes, `RESPAWN` state, and `BRICK_DELTA` updates are authoritative outputs from the server. In `server/main.c`, tighten or annotate the current logic so those outcomes remain rooted in `start_shot` and `step_shots`, not in later client interpretation.</action>
  <verify>
    <automated>bash tests/combat_ordering_smoke.sh && bash tests/combat_world_authority_smoke.sh && make build/maze-war-server</automated>
  </verify>
  <done>Phase 3 now has executable proof that combat/world outcomes are generated server-side and published through a stable documented contract.</done>
</task>

</tasks>

<verification>
Keep `bash tests/combat_ordering_smoke.sh` green after each task. Before closing the plan, run both new smoke scripts and confirm they prove the server contract directly instead of inferring it from client behavior.
</verification>

<success_criteria>
`COMB-01`, `COMB-03`, and the server side of `WRLD-01` are satisfied for Phase 3 when the server's same-tick order is explicit, documented, and proven by executable smoke coverage for shot spawn, brick mutation, hit, score, and respawn behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-combat-and-world-authority/03-01-SUMMARY.md`
</output>
