---
phase: 03-combat-and-world-authority
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - clients/linux/main.c
  - clients/linux/sdl_main.c
  - tests/combat_client_parity_smoke.sh
autonomous: true
requirements:
  - COMB-04
  - WRLD-01
  - WRLD-02
must_haves:
  truths:
    - "Linux and Atari consume the same frozen combat/world contract, so identical input sequences do not produce different visible shot behavior."
    - "Linux clients treat shot, respawn, brick, and score state as authoritative world outputs, not local derivations."
    - "Phase 3 closes only after automated parity smokes are green and the real mixed Atari/Linux checkpoint approves visible origin and outcome parity."
  artifacts:
    - path: "clients/linux/main.c"
      provides: "Deterministic Linux protocol parity path"
      contains: "SHOT, RESPAWN, BRICK_DELTA, SNAPSHOT handling aligned to the frozen contract"
    - path: "clients/linux/sdl_main.c"
      provides: "Visual Linux parity client"
      contains: "authoritative shot, respawn, brick, and score interpretation"
    - path: "tests/combat_client_parity_smoke.sh"
      provides: "Cross-client parity coverage"
      contains: "Linux and Atari handler seam assertions"
  key_links:
    - from: "clients/linux/main.c"
      to: "clients/linux/sdl_main.c"
      via: "shared authoritative packet semantics"
      pattern: "PKT_SHOT|PKT_RESPAWN|PKT_BRICK_DELTA|PKT_SNAPSHOT"
    - from: "tests/combat_client_parity_smoke.sh"
      to: "clients/atari/maze-war.asm"
      via: "cross-client parity anchors"
      pattern: "NET_SHOT_APPLY|NET_RESP_APPLY_WRK|NET_STAGE_COMMIT"
---

<objective>
Close Phase 3 by aligning the Linux clients to the same combat/world contract as the Atari client and adding the final parity gate that leads into the real mixed-session approval.

Purpose: Phase 3 is only done when Linux and Atari interpret authoritative combat/world packets the same way and the live mixed-session checkpoint confirms visible origin plus outcome parity.
Output: Updated Linux clients, expanded parity smoke coverage, and a final manual checkpoint definition for the mixed Atari/Linux session.
</objective>

<execution_context>
@/home/mozzwald/.codex/get-shit-done/workflows/execute-plan.md
@/home/mozzwald/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-combat-and-world-authority/03-RESEARCH.md
@.planning/phases/03-combat-and-world-authority/03-VALIDATION.md
@.planning/phases/03-combat-and-world-authority/03-01-PLAN.md
@.planning/phases/03-combat-and-world-authority/03-02-PLAN.md
@clients/linux/main.c
@clients/linux/sdl_main.c
@clients/atari/maze-war.asm
@doc/protocol.md

<interfaces>
From `clients/linux/main.c`:
```c
if (n >= 6 && buf[0] == PKT_RESPAWN) { ... }
else if (n >= 6 && buf[0] == PKT_SHOT) { ... }
else if (n >= 19 && buf[0] == PKT_SNAPSHOT) { ... }
```

From `clients/linux/sdl_main.c`:
```c
if (n >= 6 && buf[0] == PKT_SHOT) { ... }
if (n >= 6 && buf[0] == PKT_RESPAWN) { ... }
if (n >= 19 && buf[0] == PKT_SNAPSHOT) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align Linux packet interpretation with the frozen combat/world contract</name>
  <files>clients/linux/main.c, clients/linux/sdl_main.c, tests/combat_client_parity_smoke.sh</files>
  <read_first>clients/linux/main.c, clients/linux/sdl_main.c, .planning/phases/03-combat-and-world-authority/03-RESEARCH.md, tests/combat_client_parity_smoke.sh</read_first>
  <acceptance_criteria>
    - `clients/linux/main.c` and `clients/linux/sdl_main.c` interpret `SHOT`, `RESPAWN`, `BRICK_DELTA`, and `SNAPSHOT` in a way that matches the documented same-tick contract.
    - `tests/combat_client_parity_smoke.sh` verifies both Linux sources and the Atari source for the expected contract anchors.
    - `make build/maze-war-client build/maze-war-client-sdl` stays green.
  </acceptance_criteria>
  <action>Update `clients/linux/main.c` and `clients/linux/sdl_main.c` so their combat/world packet handling mirrors the frozen contract from `03-01` and the Atari-side constraints from `03-02`. Keep them authoritative-only: no Linux-local shot simulation, no local score/respawn derivation, and no interpretation that would let move-then-fire or turn-then-fire differ visibly from Atari. Expand `tests/combat_client_parity_smoke.sh` so it asserts the Linux handlers and Atari handlers all still anchor on the same packet types and authoritative seams.</action>
  <verify>
    <automated>bash tests/combat_client_parity_smoke.sh && bash tests/combat_ordering_smoke.sh && make build/maze-war-client build/maze-war-client-sdl</automated>
  </verify>
  <done>The Linux clients now follow the same authoritative combat/world contract as the Atari client, and automated parity coverage checks the shared seams.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve live Atari/Linux combat and world parity</name>
  <files>none</files>
  <read_first>.planning/phases/03-combat-and-world-authority/03-VALIDATION.md, tests/combat_ordering_smoke.sh, tests/combat_world_authority_smoke.sh, tests/combat_client_parity_smoke.sh</read_first>
  <acceptance_criteria>
    - In a mixed session with 1 Atari client, 1 Linux client, and 2 AI zombies, move-then-fire and turn-then-fire produce the same visible result on Atari and Linux.
    - Atari bullets visibly originate from the currently visible wizard location and facing.
    - Score, death, respawn, and brick-destruction outcomes match server-authoritative behavior on both clients.
  </acceptance_criteria>
  <action>After all automated checks are green, run the real mixed-session checkpoint. Start 1 Atari client, 1 Linux client, and 2 AI zombies. On Atari and Linux in turn, reproduce move-then-fire and turn-then-fire sequences, including one case near a brick and one kill/respawn case. Approve only if both clients show the same visible shot direction and outcome, Atari bullets originate from the currently visible wizard, and score/death/respawn/brick state stays aligned with server truth.</action>
  <what-built>Linux and Atari clients now consume one shared authoritative combat/world contract, with parity smoke coverage guarding shot, brick, score, death, and respawn interpretation before the real mixed-session check.</what-built>
  <how-to-verify>
    1. Run `bash tests/combat_ordering_smoke.sh`.
    2. Run `bash tests/combat_world_authority_smoke.sh`.
    3. Run `bash tests/combat_client_parity_smoke.sh`.
    4. Run `make all`.
    5. Start the mixed session with 1 Atari client, 1 Linux client, and 2 AI zombies.
    6. On Atari, reproduce move-then-fire and turn-then-fire sequences, including one shot into a destructible brick and one kill/respawn case.
    7. On Linux, reproduce the same move-then-fire and turn-then-fire sequences against comparable geometry.
    8. Confirm Atari bullets always originate from the currently visible wizard location and facing.
    9. Confirm Atari and Linux show the same visible shot direction, hit/no-hit result, brick destruction, score increment, death masking, and final respawn outcome.
  </how-to-verify>
  <verify>
    <automated>bash tests/combat_ordering_smoke.sh && bash tests/combat_world_authority_smoke.sh && bash tests/combat_client_parity_smoke.sh && make all</automated>
  </verify>
  <done>Phase 3 has real mixed-session approval that Linux and Atari now honor the same authoritative combat/world contract.</done>
  <resume-signal>Type `approved` if the mixed Atari/Linux checkpoint passes, or describe the exact remaining combat/world divergence and the input sequence that reproduced it.</resume-signal>
</task>

</tasks>

<verification>
Before the real checkpoint, rerun all three Phase 3 smoke scripts plus `make all`. Do not close the phase without explicit manual approval from the mixed Atari/Linux session.
</verification>

<success_criteria>
`COMB-04`, `WRLD-01`, and `WRLD-02` are satisfied for Phase 3 when Linux and Atari consume the same authoritative combat/world contract and the live mixed-session checkpoint confirms visible shot, brick, score, death, and respawn parity.
</success_criteria>

<output>
After completion, create `.planning/phases/03-combat-and-world-authority/03-03-SUMMARY.md`
</output>
