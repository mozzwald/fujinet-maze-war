---
phase: 03-combat-and-world-authority
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - clients/atari/maze-war.asm
  - tests/combat_client_parity_smoke.sh
autonomous: true
requirements:
  - COMB-02
  - WRLD-02
must_haves:
  truths:
    - "Atari draws authoritative shots only when they agree with the currently visible authoritative wizard and respawn state for that slot."
    - "Atari score, death, and respawn presentation stays derived from authoritative snapshot and respawn packets."
    - "Automated coverage proves the Atari packet-consumption seams exist at the expected snapshot/shot/respawn/world boundaries."
  artifacts:
    - path: "clients/atari/maze-war.asm"
      provides: "Visible-origin and authoritative combat/world consume path for Atari"
      contains: "NET_STAGE_COMMIT, NET_RESP_COMMIT, NET_SHOT_APPLY, guard/defer logic if needed"
    - path: "tests/combat_client_parity_smoke.sh"
      provides: "Grepped Atari/Linux contract wiring coverage"
      contains: "NET_SHOT_APPLY, NET_RESP_APPLY_WRK, SHOT/RESPAWN handlers"
  key_links:
    - from: "clients/atari/maze-war.asm"
      to: "NET_STAGE_COMMIT"
      via: "visible authoritative local actor state"
      pattern: "NET_LOCAL_PID|NET_PX_X|NET_PX_Y|NET_PJOY"
    - from: "clients/atari/maze-war.asm"
      to: "NET_SHOT_APPLY"
      via: "authoritative shot draw path"
      pattern: "NET_SHOT_WRK|NET_RESP_WRK|NET_DEAD_MASK|NET_ERASE_MASK"
---

<objective>
Align the Atari client's combat/world consume path to the frozen Phase 3 contract so bullets visually originate from the currently visible authoritative wizard state and death/respawn/score outcomes stay in sync with server truth.

Purpose: Phase 2 stabilized local movement. Phase 3 now needs the Atari presentation path to stop treating `SHOT`, `RESPAWN`, and `SNAPSHOT` as unrelated streams when visible combat state is on screen.
Output: `clients/atari/maze-war.asm` aligned to the authoritative combat/world contract plus smoke coverage in `tests/combat_client_parity_smoke.sh`.
</objective>

<execution_context>
@/home/mozzwald/.codex/get-shit-done/workflows/execute-plan.md
@/home/mozzwald/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-combat-and-world-authority/03-RESEARCH.md
@.planning/phases/03-combat-and-world-authority/03-VALIDATION.md
@.planning/phases/03-combat-and-world-authority/03-01-PLAN.md
@clients/atari/maze-war.asm
@doc/protocol.md

<interfaces>
Existing Atari seams:
```asm
NET_STAGE_COMMIT
    LDA NET_STAGE_LOCAL_PID
    STA NET_LOCAL_PID
    ...
    JSR NET_LOCAL_ACK_DISCARD
    JSR NET_LOCAL_REPLAY_PENDING
```

```asm
NET_RESP_COMMIT
    ...
    JSR NET_RESP_APPLY_WRK
```

```asm
NET_SHOT_APPLY
    ...
    JSR NET_SHOT_PTR
    ...
    JSR SETMOVE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tie Atari shot rendering to visible authoritative actor state</name>
  <files>clients/atari/maze-war.asm, tests/combat_client_parity_smoke.sh</files>
  <read_first>clients/atari/maze-war.asm, .planning/phases/03-combat-and-world-authority/03-RESEARCH.md, .planning/phases/03-combat-and-world-authority/03-01-PLAN.md</read_first>
  <acceptance_criteria>
    - `clients/atari/maze-war.asm` makes `NET_SHOT_APPLY` depend on the live authoritative-visible state for the emitting slot rather than only raw packet coordinates.
    - The Atari shot path refuses or defers stale/contradictory draws while the slot is in authoritative dead/erase state.
    - `tests/combat_client_parity_smoke.sh` greps the Atari source for the concrete shot/snapshot/respawn seams used by the frozen contract.
  </acceptance_criteria>
  <action>Update `clients/atari/maze-war.asm` so `NET_SHOT_APPLY` consults the slot's current authoritative-visible state before drawing: live authoritative position/facing committed from `NET_STAGE_COMMIT`, plus respawn/dead masking committed from `NET_RESP_COMMIT`. If a shot packet would render from a state the user cannot currently see, guard or defer the draw rather than letting it appear from a stale cell or stale facing. Create `tests/combat_client_parity_smoke.sh` as a shell script that runs `make build/maze-war-client` and greps the Atari source for the exact authoritative shot/respawn/snapshot seams used by this contract.</action>
  <verify>
    <automated>bash tests/combat_client_parity_smoke.sh && make build/maze-war-client</automated>
  </verify>
  <done>The Atari client now has an explicit authoritative-visible origin rule for shots instead of trusting packet timing alone.</done>
</task>

<task type="auto">
  <name>Task 2: Keep Atari death, respawn, and score presentation authoritative</name>
  <files>clients/atari/maze-war.asm, tests/combat_world_authority_smoke.sh</files>
  <read_first>clients/atari/maze-war.asm, .planning/phases/03-combat-and-world-authority/03-VALIDATION.md, .planning/phases/03-combat-and-world-authority/03-RESEARCH.md</read_first>
  <acceptance_criteria>
    - Atari score display still updates only from authoritative snapshot score bytes.
    - Atari death/pending-respawn and final respawn visibility follow `RESPAWN` state without stale body or shot artifacts being left behind by the new shot-origin guard path.
    - `make build/maze-war-client` stays green and `tests/combat_world_authority_smoke.sh` remains compatible with the client-side changes.
  </acceptance_criteria>
  <action>Adjust the Atari authoritative consume path so the new visible-origin shot logic coexists cleanly with existing score and respawn state: keep scoreboard digits rooted in snapshot bytes, keep dead/erase masks and final spawn commit rooted in `RESPAWN`, and ensure clearing/guard logic does not reintroduce stale actor or shot debris. Do not add local combat simulation; only tighten authoritative consumption and presentation ordering.</action>
  <verify>
    <automated>bash tests/combat_world_authority_smoke.sh && bash tests/combat_client_parity_smoke.sh && make build/maze-war-client</automated>
  </verify>
  <done>The Atari client's visible combat outcomes now stay anchored to authoritative score and respawn state while respecting the visible bullet-origin rule.</done>
</task>

</tasks>

<verification>
Keep `make build/maze-war-client` green after each task. Before closing the plan, rerun `bash tests/combat_client_parity_smoke.sh` and confirm the Atari source now exposes a clear authoritative shot/respawn/snapshot contract instead of isolated packet handlers.
</verification>

<success_criteria>
`COMB-02` and the Atari-facing part of `WRLD-02` are satisfied when Atari bullets originate from the currently visible authoritative wizard state and score/death/respawn presentation stays rooted in server packets.
</success_criteria>

<output>
After completion, create `.planning/phases/03-combat-and-world-authority/03-02-SUMMARY.md`
</output>
